services:
  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - .:/notebooks
    environment:
      - GRANT_SUDO=yes
    user: root

volumes:
  notebooks:
